lista_aula = []
for presenca in range(1,20):
    nomes = str(input('digite o nome do aluno'))
    lista_aula.append(nomes)
    if nomes == 'fila':
      lista_aula.remove('fila')
      print(lista_aula)
      break
